import { pgTable } from "drizzle-orm/pg-core";

export const usersTable = pgTable("users", {
  // TODO: Create the schema for the users table
});

export const expensesTable = pgTable("expenses", {
  // TODO: Create the schema for the expenses table
});

export const budgetsTable = pgTable("budget", {
  // TODO: Create the schema for the budgets table
});

export const categoriesTable = pgTable("categories", {
  // TODO: Create the schema for the categories table
});

export const paymentsTable = pgTable("payments", {
  // TODO: Create the schema for the payments table
});
